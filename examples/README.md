# Examples

This is a Vite project with examples of the React Sunburst components.

To setup the project, open a terminal and navigate to this folder. Run ```npm install``` to install dependencies.
Then run ```npm run dev``` to start a Vite dev server. In your browser navigate to the localhost url.